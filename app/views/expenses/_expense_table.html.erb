
<div class="card p-3 shadow-sm">
  <h5>Expenses</h5>

  <table class="table">
    <thead>
      <tr>
        <th>Date</th>
        <th>Category</th>
        <th>Amount</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @expenses.each do |expense| %>
        <tr>
          <td><%= expense.date %></td>
          <td><%= expense.final_category %></td>
          <td>₹<%= expense.amount %></td>
          <td>
            <%= link_to "Delete",
                money_cycle_expense_path(@money_cycle, expense),
                method: :delete,
                class: "btn btn-sm btn-outline-danger" %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>